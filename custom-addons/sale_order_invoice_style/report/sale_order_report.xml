<odoo>
    <!-- Report definition first -->
    <record id="sale_order_invoice_style_report" model="ir.actions.report">
        <field name="name">Invoice Style Sale Order</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">sale_order_invoice_style.custom_invoice_document</field>
        <field name="report_file">sale_order_invoice_style.custom_invoice_document</field>
        <field name="print_report_name">'Invoice-%s' % (object.name or '')</field>
    </record>
    <!-- Then the view inheritance -->
    <record id="view_order_form_inherit_print_button" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.print.button</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="%(sale_order_invoice_style.sale_order_invoice_style_report)d" string="Print Invoice Style"
                        type="action" class="btn-primary" groups="sales_team.group_sale_salesman"/>
            </xpath>
        </field>
    </record>
</odoo>
